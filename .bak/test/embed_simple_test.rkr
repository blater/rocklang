include "test/Assert.rkr"

@embed c
  int multiply(int a, int b) { return a * b; }
@end c

sub main() {
  dim x: int := 5;
  dim y: int := 7;
  dim result: int := multiply(x, y);
  AssertEQ("use embedded function", 35, result);
}

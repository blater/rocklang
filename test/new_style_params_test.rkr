include "Assert.rkr"

record Point {
  int x,
  int y
}

sub Point.distance(int dx, int dy) returns int {
  return this.x + dx + this.y + dy;
}

sub add(int x, int y) returns int {
  return x + y;
}

sub greet(string name) returns string {
  return concat("hello ", name);
}

sub first(string[] arr) returns string {
  return get(arr, 0);
}

sub mixed(int x, int y) returns int {
  return x + y;
}

sub main() returns void {
  Assert a;
  a.AssertEQ("int params", 7, add(3, 4));
  a.AssertEquals("string param", "hello world", greet("world"));

  Point p := {x := 1, y := 2};
  a.AssertEQ("record param", 13, p.distance(5, 5));

  string[] words;
  append(words, "rock");
  append(words, "on");
  a.AssertEquals("array param", "rock", first(words));

  a.AssertEQ("mixed style params", 9, mixed(4, 5));
}

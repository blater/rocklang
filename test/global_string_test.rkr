include "Assert.rkr"
Assert a;

string greeting := "hello";

sub main() {
  a.AssertEquals("global init", "hello", greeting);
  greeting := "world";
  a.AssertEquals("global reassign", "world", greeting);
}

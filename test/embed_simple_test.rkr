include "Assert.rkr"
Assert a;

@embed c
  int multiply(int a, int b) { return a * b; }
@end c

sub main() {
  int x := 5;
  int y := 7;
  int result := multiply(x, y);

  a.AssertEQ("use embedded function", 35, result);
}

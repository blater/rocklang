include "Assert.rkr"

// Host smoke test for cls(). Piped stdout → fallback branch; we only
// assert the call returns normally.

sub main() {
  Assert a;
  cls();
  print(0, 0, "after cls");
  a.AssertEQ("cls smoke", 1, 1);
}

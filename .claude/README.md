# Claude Project State

This directory holds project-level Claude context that is useful for maintaining Rock.

Committed:

- `projects/-Users-blater-src-rock/memory/` - durable project notes that should travel with the repo.

Ignored:

- `settings.local.json` - local permission/session state.
- `worktrees/` - generated Claude worktrees and build outputs.
